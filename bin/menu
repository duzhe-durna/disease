#!/usr/bin/env sh

theme=
theme_path="$HOME/.config/wmenu/theme"
run=wmenu

if [ -f "$theme_path" ]; then
    theme=$(cat "$theme_path")
fi


if [ "$1" = "run" ]; then
    shift
    run=wmenu-run
fi

$run -i $theme $@
